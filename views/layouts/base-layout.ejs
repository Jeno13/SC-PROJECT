<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>
        <%= title %>
    </title>
    <!-- Bootstrap core CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">

    <!-- Custom fonts for this template-->
    <link rel="stylesheet"
        href="https://startbootstrap.github.io/startbootstrap-sb-admin-2/vendor/fontawesome-free/css/all.min.css">


    <!-- Custom styles for this layout -->
    <link rel="stylesheet" href="/css/dashboard.css">
    <link rel="stylesheet" href="/css/base.css">
</head>

<body>
    <header class="bg-white mb-4 static-top shadow py-3 d-flex align-items-center header">
        <div class="container">
            <div class="row">
                <div class="col d-flex justify-content-between align-items-center">
                    <a class="logo text-black" href="/">
                        Chance
                    </a>
                    <div>
                        <% if (authenticated) { %>
                            <!-- 
                            Bootstrap dropdown
                            https://getbootstrap.com/docs/5.3/components/dropdowns/
                        -->
                            <div class="dropdown">
                                <button type="button"
                                    class="dropdown-toggle d-flex justify-content-end align-items-center gap-2 text-decoration-none btn btn-link"
                                    data-bs-toggle="dropdown" aria-expanded="false">
                                    <div>
                                        <img src="<%= user.profilePicture ?  '/uploads/users/' + user.profilePicture : ' /images/student.svg' %>" width="24" height="24" class="mr-1 rounded-circle">
                                        <span class="d-lg-inline text-muted">
                                            <%= user.name %>
                                        </span>
                                        <span class="badge text-bg-secondary fw-normal text-capitalize">
                                            <%= user.type %>
                                        </span>
                                    </div>
                                </button>
                                <ul class="dropdown-menu">
                                    <li><a class="dropdown-item text-black" href="/dashboard"><i class="fas fa-fw fa-tachometer-alt"></i> Dashboard</a>
                                    </li>
                                    <li><hr class="dropdown-divider"></li>
                                    <li><a class="dropdown-item text-danger" href="/logout"><i class="fas fa-fw fa-power-off"></i> Logout</a></li>
                                </ul>
                            </div>
                            <% } else { %>
                                <div class="nav d-flex gap-3">
                                    <a href="/login" class="btn btn-dark">Login</a>
                                    <a href="register" class="btn btn-dark">Register</a>
                                </div>
                                <% } %>
                    </div>
                </div>
            </div>
        </div>
    </header>

    <section>
        <div class="container">
            <form method="get" action="/search" class="bg-white p-4 shadow border rounded mb-4">
                <input type="text" class="form-control" name="project"  placeholder="Search for projects by name..."/>
            </form>
        </div>
    </section>

    <main>
        <div class="container">
            <% if(messages) { %>
                <% if (messages.success) { %>
                    <div class="alert alert-success" role="alert">
                        <%= messages.success %>
                    </div>
                <% } %>
                <% if (messages.error) { %>
                    <div class="alert alert-danger" role="alert">
                        <%= messages.error %>
                    </div>
                <% } %>
            <% } %>
        </div>
        <%- body %>
    </main>

    <!-- Bootstrap core JavaScript-->
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.8/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.min.js"></script>
</body>

</html>